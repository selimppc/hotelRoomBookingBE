<form method="post" action="{{ path('room_type_delete', {'id': room_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ room_type.id) }}">
    <button class="btn">Delete</button>
</form>
